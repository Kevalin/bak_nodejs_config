var encipher = function(key, text) {
    var cipher = require('./cipher.js')
    var ciphered = cipher.encipher(text, key)

    console.log(ciphered)
}

var myArgs = process.argv.slice(2)
encipher(myArgs[0], myArgs[1])